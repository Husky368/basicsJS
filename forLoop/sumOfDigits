function sumOfDigits(input){
    let digits = input[0]
    let sum = 0
    for (let i = 0; i < digits.length; i++) {
        let digit = digits[i]
        sum += parseInt(digit)
    }
    console.log(`The sum of the digits is:${sum}`);
}
